<link rel="stylesheet" href="static\passengerProfile.css" type="text/css"/>
<h1 class="text-center">PROFILE</h1>
<div class="container">
   
    <table class="table table-striped mt-4">
        <tr>
            <th>id</th>
            <th>firstName</th>
            <th>lastName</th>
            <th>Email</th>
            <th>Mobile</th>
            <th>DOB</th>
            <th>Action</th>
        </tr>
    
        <tr>
            <td>{{data.id}}</td>
            <td>{{data.firstName}}</td> 
            {{!-- ee data enn paranja passengercontrollerile passenger profile display cheyyanolla functionil parameter aayi passeythind --}}
            <td>{{data.lastName}}</td>
            <td>{{data.email}}</td>
            <td>{{data.mobile}}</td>
            <td>{{data.dob}}</td>

            <td>
                <a href="/update"><button class="btn btn-primary">Edit</button></a> |
                <a href="/delete" class="text-danger" onclick="return confirm('Are you sure you want to delete this')"><button class="btn btn-primary">Delete</button></a>
            </td>
        </tr>
    
    </table>
    <p class="text-end">You are logged in as: 
        <b>{{data.firstName}} {{data.lastName}}</b>
    </p>
    {{!-- <div>
        <a href="/cabs">Cabs</a>
    </div> --}}
     <a href="/cabs">
    <button class="btn btn-primary"> Cabs</button>
    </a>
</div>